# Week 39

> 2026.09.21 - 2026.09.27  
> 状态：进行中

## What I Learned

### 2026.09.21 — FreeRTOS 入门

- 通过课程示例初步学习了 RTOS 的作用。
- 接触了裸机系统与 RTOS 多任务系统的区别。
- 接触了 Task 创建、任务优先级、任务状态和调度过程。
- 在已有的 Windows FreeRTOS 模拟实验中亲自修改了 Task 延时并运行验证。
- 将高优先级 Task 的延时从 300 ms 改为 700 ms，运行前预测低优先级 Task 会运行 5 次；实际观察到 `HIGH` 运行 4 次、`LOW` 运行 5 次。

### 2026.09.22 — OpenCV 摄像头输入

- 参考课程示例，亲手输入了 OpenCV C++ 摄像头程序。
- 成功编译并运行程序，在 `Video` 窗口中看到了实时摄像头画面。
- 学习了从 `VideoCapture` 打开摄像头，到使用 `Mat` 接收图像帧，再通过 `imshow()` 和 `waitKey()` 持续显示画面的基本流程。
- 本次只测试了摄像头输入，没有测试视频文件输入。

### 2026.09.23 — OpenCV 摄像头源码复盘

- 结合前一天亲手输入的代码，复盘了 `CommandLineParser`、`VideoCapture`、`Mat`、`namedWindow()`、`imshow()`、`waitKey()` 和 `release()` 的职责。
- 区分了 `open(int)` 与 `open(const cv::String&)` 两种重载，并理解了摄像头编号与视频文件路径分别对应的输入方式。
- 明确了 `isOpened()` 只检查视频源是否成功打开，循环中还应检查每次取帧是否成功。

## What I Built

- 运行了 VS Code + CMake 管理的 FreeRTOS Task 基础实验。
- 完成了一次 Task 延时参数修改和结果验证；本次没有从零创建 Task。
- 亲手输入并成功运行了一个 OpenCV 摄像头读取与显示示例。

## Problems

1. 还不能完整说明 `xTaskCreate()` 的参数。
2. 对任务状态切换存在误解，最初认为切换任务必须经过 `Blocked → Suspended`。
3. 对裸机与 RTOS 的区别还停留在“顺序执行与交叉执行”的初步理解。
4. 虽然知道提高优先级不能让 Blocked Task 提前结束延时，但还需要继续巩固其原因。
5. 最初把 `waitKey(30)` 主要理解成让画面停留，忽略了它还负责窗口刷新和键盘事件处理。
6. 最初知道 `isOpened()` 不能代替逐帧读取检查，但还不能解释两者检查阶段的区别。

## What I Learned From These Problems

1. 单核处理器同一时刻通常只有一个 Task 处于 Running，多个任务由调度器快速切换。
2. 调度器从 Ready Task 中选择任务运行，优先级是重要依据。
3. Blocked 表示等待时间或事件；Suspended 表示任务被挂起，两者不是任务切换必须依次经过的状态。
4. RTOS 提供调度、通信和同步机制，但不会自动保证系统稳定。
5. 优先级只在 Ready Task 之间比较，不能缩短 `vTaskDelay()` 产生的等待时间。
6. 延时更短的低优先级 Task 可能先从 Blocked 回到 Ready，并在高优先级 Task 仍然 Blocked 时运行。
7. `cap.read(frame)` 或 `cap >> frame` 负责取图，`imshow()` 负责提交显示请求，`waitKey()` 负责窗口事件处理。
8. `isOpened()` 检查输入源是否成功打开，`read()` 检查当前这一帧是否成功取得，两者不能相互代替。
9. `open(0)` 与 `open(videoFile)` 会根据整数和字符串参数选择不同的重载版本。

## Best Thing This Week

- 第一次亲自修改并运行 FreeRTOS Task 实验，而且对 `LOW` 的运行次数预测与实际结果一致。

## Next Steps

1. 复习并完整说明 `xTaskCreate()` 的六个参数。
2. 修改任务优先级，预测并观察调度结果。
3. 尝试亲自新增一个 Task。
4. 继续结合实验复习 Ready、Running、Blocked 和 Suspended。
